### taking a look to dynamic routes
using [] to dynamically generate routes

## by using this we can get rid ou of creating multiple routes with same layout 

`layout.js` file is used inside [productId] folder for different layout however root layout is also showing 
### called Nested Layout