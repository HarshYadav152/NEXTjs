`searchParams` is a promise resolve containing query parameter which are stating ?lang=hn

for using await in other words since searchParams is client component thing we have to use {use} from react

`searchParams` not accessible in layout files