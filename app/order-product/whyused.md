`useRouter` form navigating programmatically using router.push("/any path") and router.many methods
- in this we can also use forward back and preserve history or not like `replace` in Link

also `redirect` using redirect("/path name")